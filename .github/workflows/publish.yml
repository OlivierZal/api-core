# Calls the family's reusable publish pipeline (pack, attest the
# tarball's provenance, publish to GitHub Packages); the `npm`
# environment travels with the called job.
jobs:
  publish:
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    uses: OlivierZal/configs/.github/workflows/reusable-publish.yml@9b69e8520dfd11484df292ca75c10d51beba75ca # v6.3.1
name: Publish package to GitHub Packages
on:
  release:
    types: [published]
permissions: {}
